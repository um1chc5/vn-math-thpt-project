# Output

Default: generated đề stay local (gitignored).

**Examples** under `lop12/` (`.tex` + `.pdf` only) may be committed on the test/demo branch so teachers can open a sample without regenerating. Build junk (`.aux`, `.log`, `.xdv`, …) stays ignored.

Regenerate anytime with the exam CLI; overwrite these snapshots when the sample should change.
